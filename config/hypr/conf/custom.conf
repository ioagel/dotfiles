# Add your additional Hyprland configurations here
#
# This is an additional key binding
# bind = $mainMod CTRL, up, workspace, empty
#
# Example for xwayland
xwayland {
  force_zero_scaling = true
}

# qt5ct environment variable
# env = QT_QPA_PLATFORMTHEME,qt5ct
# env = QT_QPA_PLATFORM,xcb
env = GDK_SCALE,1.35
env = GDK_DPI_SCALE,1.35

### OVERRIDES ###
# override the default inactive opacity of 0.8
decoration {
  inactive_opacity = 1.0
}
### END OVERRIDES ###

### Autostart ###
exec-once = sleep 5 && /usr/bin/1password
exec-once = sleep 5 && ~/.bin/synology-drive-wayland
exec-once = sleep 5 && /usr/bin/redshift-gtk

### Window Rules ###
windowrule = center,float,^(cloud-drive-ui)$
windowrule = float,^(jetbrains-toolbox)$
