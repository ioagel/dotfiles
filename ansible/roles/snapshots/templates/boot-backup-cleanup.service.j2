[Unit]
Description=Clean up old boot backups
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/cleanup-boot-backups --keep-count={{ boot_backup_keep_count }}

[Install]
WantedBy=multi-user.target
