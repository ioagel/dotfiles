#!/bin/sh

BAT_CMD=bat

if command -V batcat >/dev/null; then
  # We are in Ubuntu
  BAT_CMD=batcat
fi

if grep 'day' <~/.terminal-theme; then
  # We are in 'day' mode
  $BAT_CMD --theme OneHalfLight "$@"
  exit 0
fi

$BAT_CMD "$@"
